#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../varstream/cli/json2varstream.js" "$@"
  ret=$?
else 
  node  "$basedir/../varstream/cli/json2varstream.js" "$@"
  ret=$?
fi
exit $ret
